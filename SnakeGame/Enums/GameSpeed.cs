namespace SnakeGame
{
        //TimeSpan values
        internal enum GameSpeed
        {
            Fast = 20,
            Moderate = 50,
            Slow = 100,
            DamnSlow = 500
        };
}
